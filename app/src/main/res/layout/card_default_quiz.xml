<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    app:cardCornerRadius="8dp">

    <!-- Card Wrapper -->
    <LinearLayout
        android:id="@+id/card_default"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <!-- Left Wrapper -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="6"
            android:orientation="vertical"
            android:padding="10dp">

            <TextView
                android:id="@+id/card_default_question_title"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:ellipsize="end"
                android:fontFamily="@font/font_regular"
                android:includeFontPadding="false"
                android:singleLine="true"
                android:text="Quiz"
                android:textColor="@color/black"
                android:textSize="24sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/card_default_question_total_questions"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_marginBottom="30dp"
                android:ellipsize="end"
                android:fontFamily="@font/font_regular"
                android:includeFontPadding="false"
                android:singleLine="true"
                android:text="20 question(s)"
                android:textColor="@color/light_gray"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/card_default_question_participants"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:ellipsize="end"
                android:fontFamily="@font/font_regular"
                android:includeFontPadding="false"
                android:singleLine="true"
                android:text="Joined by 1000 Binusian"
                android:textColor="@color/orange"
                android:textSize="14sp"
                android:textStyle="bold" />

        </LinearLayout>

        <!-- Quiz Color -->
        <View
            android:id="@+id/card_default_question_color"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:background="@color/light_blue" />

    </LinearLayout>

</androidx.cardview.widget.CardView>